@model ApplicationPhoto.Web.UI.Models.ViewModel.FilesPhotoViewModel
@{
    ViewData["Title"] = "Ajouter photo";
   var listItemsVoyage = this.ViewBag.ListeVoyage;
    var listItemsCategorie = this.ViewBag.ListeCategorie;
}
			<div class="row">
				<div class="col-md-5 col-md-pull-7">
						<div class="booking-form">
						 
								<form asp-action="Create" enctype="multipart/form-data" asp-action="Upload">
								
										
									<div class="form-group">
										<label asp-for="@Model.Photos.ImageUrl" class="form-label">Insérer image</label>
					<input asp-for="@Model.Files" class="form-control" multiple />
					 
					<span asp-validation-for="@Model.Files" class="text-danger"></span>
										
									</div>
									<div class="row">
									  <div class="form-group">
										  <label  class="control-label">Catégorie</label>
						<select asp-for="@Model.Photos.CategorieId" asp-items="listItemsCategorie"></select>
									</div>
								</div>
								<div class="row">
									  <div class="form-group">
										  <label  class="control-label">Voyage</label>
						<select asp-for="@Model.Photos.VoyageId" asp-items="listItemsVoyage"></select>
									</div>
								</div>
								<div class="row">
									<div class="form-group">
						<label asp-for="@Model.Photos.Description" class="control-label"></label>
						<textarea cols="5" rows="6" asp-for="@Model.Photos.Description" class="form-control"></textarea>
										<span asp-validation-for="@Model.Photos.Description" class="text-danger"></span>
									</div>
								</div>
								@{
									if (ViewBag.LatLon ==false){
										<div class="row">
									<div class="form-group">
										<label class="control-label">La photo ne contient pas de coordonnées GPS, selectionnez une adresse (fonctionnalité à venir)</label>
										<label class="control-label"></label>
								<input type="date" asp-for="@Model.Photos.Description" class="form-control" placeholder="Fonctionnalité pas encore disponible" />
										<span asp-validation-for="@Model.Photos.Description" class="text-danger"></span>
									</div>
								</div>

									}
									if (ViewBag.Date ==false){
										<div class="row">
									<div class="form-group">
										<label class="control-label">La photo ne contient pas de date, veuillez en saisir une</label>
								<label asp-for="@Model.Photos.DatePicture" class="control-label"></label>
								<input type="date" asp-for="@Model.Photos.DatePicture" class="form-control" />
										<span asp-validation-for="@Model.Photos.DatePicture" class="text-danger"></span>
									</div>
								</div>

									}
								}
								<div class="form-btn">
										<input type="submit" value="Ajouter Image" class="submit-btn" />
									</div>
							
							</form>
						</div>
					</div>
					<div class="col-md-7 col-md-push-5">
						<div class="booking-cta">
							<h1>Ajouter votre photo</h1>
							<p>Ajouter votre photo dans votre portofolio de voyage, vous pouvez saisir un catégorie afin de
								retrouver plus facilement vos souvenirs et ajouter un description pour ne rien oublier de vos plus
								instants
							</p>
						</div>
					</div>

				
	
	</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
